import { ExpireMap } from '../index';

let map: ExpireMap;

describe('Testing ExpireMap', () => {
  beforeAll(async () => {
    map = new ExpireMap();
  });

  test('Set object and immediately get', async () => {
    
  });

});

